name: Build OpenWrt

on:
  workflow_dispatch:
    inputs:
      configuration:
        description: 'Configuration'
        required: true
        default: ''
        type: choice
        options:
          - x86_64-24.10-pw
      runner:
        description: 'Runner'
        required: true
        default: 'ubuntu-20.04'
        type: choice
        options:
          - ubuntu-20.04
          - ubuntu-22.04
          - ubuntu-24.04

jobs:
  build:
    runs-on: ${{ github.event.inputs.runner }}

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Prepare environment
        run: |
          echo "CONFIG_PATH=${{ github.workspace }}/${{ github.event.inputs.configuration }}" >> $GITHUB_ENV
          echo "BUILD_ROOT=${{ github.workspace }}/openwrt" >> $GITHUB_ENV
          echo "DEPENDENCIES=depends-${{ github.event.inputs.runner }}" >> $GITHUB_ENV
          grep -v -e '^#' -e '^\s*$' $CONFIG_PATH/.env >> $GITHUB_ENV

      - name: Maximize disk space
        uses: easimon/maximize-build-space@master
        with:
          remove-dotnet: true
          remove-android: true
          remove-haskell: true
          remove-codeql: true
          remove-docker-images: true

      - name: Setup build system
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install $(cat $DEPENDENCIES | xargs)
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir
          cd /workdir
          git clone $REPO_URL -b $REPO_BRANCH openwrt
          ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt
          df -hT $PWD

      - name: Prepare Compile
        run: |
          chmod +x $CONFIG_PATH/patch_*.sh || :
          [ -f $CONFIG_PATH/feeds.conf.default ] && mv $CONFIG_PATH/feeds.conf.default $BUILD_ROOT/feeds.conf.default
          [ -f $CONFIG_PATH/patch_feed.sh ] && $CONFIG_PATH/patch_feed.sh || :
          $BUILD_ROOT/scripts/feeds update -a
          $BUILD_ROOT/scripts/feeds install -a
          [ -f $CONFIG_PATH/patch_package.sh ] && $CONFIG_PATH/patch_package.sh || :
          [ -d $CONFIG_PATH/files ] && mv $CONFIG_PATH/files $BUILD_ROOT/files
          [ -f $CONFIG_PATH/.config ] && mv $CONFIG_PATH/.config $BUILD_ROOT/.config

      - name: Download package
        run: |
          cd openwrt
          ls -al
          cat feeds.conf.default
